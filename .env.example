# .env.example — Monorepo root
# Phase 4 — Backend Core
#
# Copy to server/.env (or client/.env for client vars) and fill in real values.
# NEVER commit .env files to version control.

# =============================================================
# CLIENT (client/.env)
# =============================================================

# RPC URL used by ethers.js in the frontend (optional — MetaMask injects its own)
VITE_BLOCKCHAIN_RPC_URL=http://127.0.0.1:8545

# Backend API base URL consumed by the React client
VITE_API_BASE_URL=http://localhost:4000

# =============================================================
# SERVER (server/.env)
# =============================================================

# TCP port the Express API listens on
PORT=4000

# Node environment: development | production | test
NODE_ENV=development

# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/blockchain-secure-docs

# Comma-separated list of allowed CORS origins (React dev server URLs)
CLIENT_ORIGIN=http://localhost:5173,http://localhost:5174

# JSON-RPC endpoint for local Hardhat / public testnet (Phase 5+)
BLOCKCHAIN_RPC_URL=http://127.0.0.1:8545

# IPFS HTTP API base URL (Phase 7)
IPFS_API_URL=http://127.0.0.1:5001

# =============================================================
# BLOCKCHAIN (blockchain/.env — Phase 5)
# =============================================================

# Private key for contract deployments (local dev only — never a mainnet key)
# DEPLOYER_PRIVATE_KEY=0xabc123...

# Etherscan API key for contract verification (Phase 9)
# ETHERSCAN_API_KEY=
